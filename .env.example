# Шаблон настроек: скопируйте в .env (copy .env.example .env)
# и заполните реальными значениями.
# Сам .env содержит секреты и в git не попадает (см. .gitignore).

SMTP_HOST=smtp.mail.ru
SMTP_PORT=465
SMTP_USER=CHANGE_ME
SMTP_PASSWORD=CHANGE_ME
MANAGER_EMAIL=CHANGE_ME
